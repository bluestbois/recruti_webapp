{{ form_start(form,{'attr':{'novalidate': 'novalidate'}}) }}
    {{ form_widget(form.content,{'attr': {'class': 'form-control w-100 mt-2 mb-2 tag ','placeholder': 'Content'}})}}

<div style="color :red;" >
    {{form_errors(form.content)}}
</div>

{{ form_widget(form.rating,{'attr': {'class': 'form-control w-100 mt-2 mb-2','placeholder' : 'how was the quality of the post 0 to 5 '}}) }}
<div style="color :red;" >
    {{form_errors(form.rating)}}
</div>

<div style="width: 100%; display:flex; justify-content: flex-end">
    <button class="btn btn-primary btn-sm shadow-sm mt-2 mb-2">
        <i class="fas fa-{{ icon|default('save') }}"></i>
        {{ button_label|default('Save') }}
    </button>
</div>
{{ form_end(form) }}
