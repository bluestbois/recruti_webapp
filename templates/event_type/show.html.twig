{% extends 'base.html.twig' %}

{% block title %}EventType{% endblock %}
{% block gobackbtn %}
    <a href="{{ path("event_type_index") }}" class="d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm">
        <i class="fas fa-arrow-left fa-sm text-white-50"></i> Go back
    </a>
{% endblock %}

{% block page_title %}
    Event type
{% endblock %}
{% block main %}
    <h1>EventType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ event_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ event_type.name }}</td>
            </tr>
        </tbody>
    </table>
    <div style="width: 100%; display:flex; justify-content: flex-end">
        <a href="{{ path('event_type_edit', {'id': event_type.id}) }}"><button type="button" class="btn-sm shadow-sm btn btn-primary mt-2 mb-2 mr-2"><i class="far fa-edit"></i> Edit</button></a>
        {{ include('event_type/_delete_form.html.twig') }}
    </div>



{% endblock %}
