{% extends 'base.html.twig' %}

{% block title %}Forum{% endblock %}

{% block main %}
    <h1>Forum</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ forum.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ forum.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ forum.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('forum_index') }}"class="btn btn-secondary"> < Back </a>

    <a href="{{ path('forum_edit', {'id': forum.id}) }}" class="btn btn-danger">edit</a>

    {{ include('forum/_delete_form.html.twig') }}
{% endblock %}
